# frontend

React PWA (Vite, TypeScript, Tailwind v4, shadcn/ui). See the repo root
`README.md` for full setup and deploy instructions.

```bash
npm install
npm run dev      # http://localhost:5173, proxies /api to the PHP backend on :8000
npm run build    # static output in dist/
npm run lint
```
